import { Gender } from "@/pages/Home/types";

export interface UpdateUser {
    // personal data
    name: string;
    birthDate: Date;
    gender: Gender;  
    // shipping data
  
    // contact data
  
    // account data
  }
  